% extends 'template';
% content header => begin

% if (scalar (keys %{$params->{files}})) {
     <h1>Доступны новые файлы для обновления</h1><br>
%    foreach my $file (keys %{$params->{files}}) {

        <%= $file %><br>
%    }
% }
% end
